{
  "name": "@elevate-ts-samples/dependency-injection",
  "version": "1.0.0",
  "description": "Cloudflare Worker with Reader monad for dependency injection",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "dependencies": {
    "elevate-ts": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "wrangler": "^3.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "build": "tsc --noEmit"
  }
}
